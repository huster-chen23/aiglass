# 开发规范（版本演进铁律）

> 来源：0908 事故复盘——v1.1 合并时视觉问答路由丢失，导致新版 AI 能力不如旧版。
> 本规范由此事故确立，**每个版本开发前必须执行第 1 条**。

---

## 铁律：新版本 = 上一版本的全量继承 + 新增

**任何新版本开工前，先完成"功能继承清单"对照，确认旧版每一项功能在新版中都有落点，
然后才允许开始新增开发。只增不删；确要删除/替换某功能，必须在继承清单中标注
"移除"并写明理由。**

---

## 一、版本开发流程（每版必走）

```text
第 1 步  继承清单：列出上一版本全部功能 → 逐项标注在新版的落点
第 2 步  冻结基线：上一版本打 git tag（如 v1.0），作为回退锚点
第 3 步  开发：在上一版本代码基础上加工，禁止从空白文件重写已验证模块
第 4 步  回归验收：继承清单逐项"✓ 实测通过"（不是"应该没问题"）
第 5 步  新功能验收：新增项逐项测试
第 6 步  提交：git commit + tag + push；更新本文件的清单存档
```

## 二、回归验收的教训（0908 事故）

- 丢失的功能：**视觉问答路由**（"你看到了什么"→ 带画面问 GLM）
- 丢失原因：合并模块时重写了输入处理，视觉关键词路由没有跟着迁移
- 为什么没被发现：只测了新功能（意图控制/对话），没有跑旧功能回归
- 修复成本：一条 VISION_KEYS 路由 + 输入分流重构
- 规避手段：本文件第三节的功能清单 + 回归验收步骤

---

## 三、功能继承清单（持续维护）

> 格式：功能 | v1.0 | v1.1 | 落点/说明。✓=实测通过。

| 功能 | v1.0 | v1.1 | 落点 |
|---|---|---|---|
| 屏幕/摄像头采集（多后端+预热+降级） | ✓ | ✓ | hr_hud_full.capture_thread |
| UDP 分片图传（GTP 帧） | ✓ | ✓ | protocol.py（不变） |
| 显示端独立窗口 | ✓ | ✓ | display_main.py（保留） |
| 心率遥测（模拟源） | — | ✓ | hr_hud_full.sim_thread |
| YOLO 物体检测四角框 | — | ✓ | hr_hud_full.detect_thread |
| **视觉问答（带画面问 GLM）** | ✓ | ✓ | 智能路由 has_vision_intent（0908 修复） |
| GLM 多轮对话 | — | ✓ | glm_chat_async |
| 界面意图控制（自然语言开关） | — | ✓ | ai_toggle |
| 语音播报（TTS） | — | ✓ | confirm_speak |
| 高心率告警 | — | ✓ | HUD 告警横幅 |
| 跨互联网中继 | ✓ | ✓ | relay_server.py（不变） |
| GTP 数据库导出/解析 | ✓ | ✓ | auto_hr.py |

## 四、新功能规划中的条目（未实现，勿忘）

| 规划项 | 目标版本 | 出处 |
|---|---|---|
| GT5 心率真机接入（A/B 验证后定路线） | v1.1 后半 | 0908 复盘 |
| HCI 抓包分析（蓝牙协议时序图） | 报告素材 | 0908 复盘 |
| 语音 ASR 实时接入（当前为控制台文字） | v1.3 | 版本规划 v1.3 |
| 队友定位（RSSI+GPS） | v1.3 | 版本规划 v1.3 |
| LoRa 远距离信标 | v1.4 | 版本规划 v1.4 |
| GLM 天气/离线地图 | v1.5 | 版本规划 v1.5 |
| 显示图层管理器（多组件） | v1.6 第一步 | 版本规划 v1.6 |
| 语音意图控制界面开关（当前为控制台文字版） | v1.6 第二步 | 版本规划 v1.6 |
| GT5 心率 Health Kit 官方接入（安卓网关 App） | v2.0 前后 | Health Kit 路线 |

---

## 五、约束

- 每个版本一个 git tag（v1.0 已打）；远程不可达时本地提交，恢复后补推
- 任何"删功能"的决定必须在继承清单标注移除理由，否则视为事故
