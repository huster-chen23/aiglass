# ⏸ 断点标记（0909 早）—— 3 小时后从这里继续

## 一句话状态
v1.0.1 代码已改完（模型切 glm-5.3-flash + 429 自动回退 glm-4-flash），
**只差：恢复额度后跑一次验收 → 提交打标 → 重启眼镜端验证**。

## 3 小时后按顺序执行

### 1. 验收（1 分钟）
```powershell
cd F:\download\桌面\jw\ai_glasses_solo
py test_api.py
```
预期：[1/2] 文本走 glm-4-flash 回退显示 OK；[2/2] 视觉 glm-4v-flash OK。
（glm-5.3-flash 若仍 429 = 资源包未就绪，回退已保证能用）

### 2. 提交打标（30 秒）
```powershell
git add -A
git commit -m "v1.0.1: 模型切换 glm-5.3-flash + 429 自动回退（验收通过）"
git tag v1.0.1
git push
```
（刚才已有一个本地提交 fd3bef8 之后的代码变更也在这次一起推）

### 3. 重启眼镜端验证（2 分钟）
```powershell
cd versions\v1.1_watch
py hr_hud_full.py
```
控制台：输入 `对话` → 随便聊一句（确认走 5.3 或回退）；`关闭心率显示` → 面板关闭+语音

### 4. 手表心率 A/B（10 秒）
打开手表"心率"App：显示数字=A（GB 同步粒度问题）／"请连接手机"=B（生态墙，转心率带）

## 关键事实备忘
- glm-5.3-flash 模型名有效，但帐号**无资源包**（429/1113）→ 控制台可领包后零改动生效
- glm-4-flash / glm-4v-flash：免费可用（实测 OK）
- 手表已重新绑定手机（运动健康），绑定时对电脑零广播（两次实测）
- GT5 地址：5C:D8:9E:6A:F0:16 · 电量约 14%（**记得充电**）
- 本地领先远程若干提交：网络好了 git push 即可
