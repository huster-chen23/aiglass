# 第一次主题任务汇报讲稿（约 5 分钟）

> 对应 PPT：《华科蓝_第一次汇报_基于UDP的AR实时图传.pptx》
> 按打分权重分配时间：科学问题凝练 90 秒 · 需求分析 60 秒 · 设计实现 70 秒 · 实验验证 60 秒 · 总结 40 秒

---

## 开场（对应 S1–S2，约 20 秒）

各位老师好，我们小组的题目是《基于 UDP 的 AR 实时图传系统》。汇报按四个部分展开：
先凝练科学问题，再做需求分析，然后是系统设计与实验验证。

## 第一部分：科学问题凝练（对应 S3–S4，约 90 秒）

我们不是先想"做什么功能"，而是先问"要解决什么网络问题"。

在不可靠信道上传输实时视频，存在一个根本矛盾：IP 网络是尽力而为的，不承诺时延
也不承诺不丢包；而人眼对连续性的要求是刚性的。把它凝练成一个核心科学问题就是：
**在 UDP 尽力而为模型上，如何以最小的协议开销，构建"实时性优先"的观看体验？**

这个大问题分解成两个可验证的子问题：

第一，**大帧超限**。单帧 JPEG 约 50KB，超过 UDP 数据报 65507 字节的上限，
而且 IP 分片不可控——任何一片丢失整包作废。所以必须在应用层设计分片与重组协议。

第二，**不可靠与实时的冲突**。无线丢包不可避免，而重传旧帧的代价是阻塞新帧。
所以需要帧级超时丢弃机制，把丢包的影响严格限制在单帧之内。

两个子问题服从同一条主线：**实时性大于完整性——旧帧不值得重传，最新画面才是价值。**

## 第二部分：需求分析（对应 S5，约 60 秒）

由科学问题推导出四项同时成立的核心需求：

R1 实时性：端到端 30 毫秒以内、20 帧连续；R2 带宽：单路约 8 兆、平稳期增量上报；
R3 穿透：在没有公网 IP 的家庭网络里跨互联网互通；R4 降级：丢包一半也不阻塞播放。

这四条决定了我们必须**自研应用层协议**——现成方案没有一条能同时满足。

## 第三部分：系统设计与实现（对应 S6–S8，约 70 秒）

架构上数据面与控制面分离：画面流走 UDP，AI 意图走 HTTPS，GLM 云端按需唤醒。

关键设计是 GTP/1.0 帧头，八个字节覆盖三件事：魔数校验过滤非法流量；
帧号加分片序号支持乱序重组；时间戳用于端到端时延测量——实测小于 4 毫秒。

配套机制是帧级超时丢弃：0.5 秒收不齐就整帧丢弃，立即播放后续帧，
把丢包的影响锁死在单帧之内。

## 第四部分：实验验证（对应 S9–S11，约 60 秒）

对照实验：同帧率、同编码，对 UDP 正常、UDP 丢包 50%、TCP 三种方式测试。

结果：UDP 正常 19.64 帧、时延 3.95 毫秒；丢包一半降到 9.56 帧，但播放不阻塞；
TCP 19.51 帧且数据完整。结论支持我们的假设：实时场景选 UDP，用应用层机制补可靠性。

另外按带宽账本推演：单路 8 兆、一千观众就是 8G 出口——单机物理不可行，
所以规模化的唯一解是房间制中继加树形分发，这正是直播平台的架构，我们做了最小原型。

## 结尾（对应 S12–S15，约 40 秒）

回到科学问题：两个子问题都得到了机制级解决并经量化验证。我们也诚实标注边界：
万级并发是设计目标尚未实测，部分遥测来自模拟源。

这个项目给我们的最大收获是：网络协议不是背出来的，是**被问题逼出来的**——
每一个字段都对应一个真实痛点。谢谢大家，欢迎批评指正。

---

## 可能的提问与应答

**Q1：为什么不用 TCP？**
实时视频旧帧到达已无价值；TCP 重传会造成队头阻塞。我们实测 TCP 帧率相当，
但公网劣化时存在卡顿风险；UDP 丢帧不阻塞。

**Q2：50% 丢包为什么只掉一半帧率而不是全断？**
丢包独立随机作用于每个数据报，约一半帧仍完整到达；未收齐的帧被超时丢弃，
不会阻塞后续帧的处理。

**Q3：为什么帧头是 8 字节，能不能再省？**
八个字节分别服务于校验、重组、时延测量三个必需功能。再省就要牺牲其中一项能力，
当前设计已是功能与开销的平衡点。

**Q4：中继服务器挂了怎么办？**
同一局域网内系统退化为直连模式不受影响；跨网场景属单点，是 v1.2 引入
多中继与调度的原因，报告中已作为边界诚实说明。

**Q5：和商用方案（如 WebRTC）比差在哪？**
WebRTC 是工业级完整栈（拥塞控制、加密、回声消除）；我们实现的是其核心思想的
最小验证原型——这正是后续学习与迭代的方向。
