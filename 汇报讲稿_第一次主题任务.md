# 第一次主题任务汇报讲稿（20 页版 · 约 7 分钟）

> 对应 PPT：《华科蓝_第一次汇报_基于UDP的AR实时图传.pptx》（20 页）
> 时间分配：开场 20s · 科学问题 70s · 选型 35s · 需求 35s · 设计实现 100s · 实验 95s · 计网 45s · 踩坑 30s · 总结 40s

---

## 开场（S1–S2，约 20 秒）

各位老师好，我们小组的题目是《基于 UDP 的 AR 实时图传系统》。汇报四个部分：
科学问题凝练、需求分析、系统设计与实现、实验验证与结论。

## 第一部分：科学问题凝练（S3–S4，约 70 秒）

我们不是先想"做什么功能"，而是先问"要解决什么网络问题"。

实时视频传输的根本矛盾在于：IP 网络的尽力而为模型不提供任何时延与完整性
保证，而人眼对视频连续性的要求是刚性的。把它凝练成一个核心科学问题：

**在 UDP 尽力而为模型上，如何以最小的协议开销，构建"实时性优先"的观看体验？**

这个大问题分解成两个可验证的子问题：

第一，**大帧超限**。单帧 JPEG 约 50KB，超过 UDP 数据报 65507 字节上限，
且 IP 分片不可控——必须在应用层设计分片与重组协议。

第二，**不可靠与实时的冲突**。无线丢包不可避免，重传旧帧的代价是阻塞新帧，
所以需要帧级超时丢弃机制。

一句话主线：**实时性大于完整性——旧帧不值得重传，最新画面才是价值。**

## 传输层选型（S5，约 35 秒）

定下主线后先回答"为什么是 UDP"。我们做了对照：UDP 无连接、8 字节头、
有报文边界，实测 19.64 帧、3.95 毫秒；TCP 可靠有序但存在粘包与队头阻塞风险，
实测 19.51 帧。结论：实时视频"要新不要全"，选 UDP，
再用应用层机制补偿它的不可靠——这就是后面 GTP 协议的动机。

## 第二部分：需求分析（S6，约 35 秒）

四项核心需求，同时成立、缺一不可：

R1 实时性：端到端 30 毫秒以内；R2 带宽：单路约 8 兆、平稳期增量上报；
R3 穿透：无公网 IP 也要跨互联网互通；R4 降级：丢包一半也不阻塞播放。

这四条决定了我们必须自研应用层协议——现成方案没有一条能同时满足。

## 第三部分：系统设计与实现（S7–S9，约 100 秒）

**架构上数据面与控制面分离**（S7）：画面流走 UDP 数据面，可经房间制中继
跨互联网；AI 意图与问答走 HTTPS 控制面，GLM 云端按需唤醒——
眼镜端、显示端、GLM 云端三个节点各司其职。

**协议上自研 GTP/1.0**（S8）：8 字节帧头覆盖三大需求——魔数校验过滤非法流量；
帧编号让接收端按帧建缓存、支持乱序；分片序号加总片数支持收齐重组、防丢防重；
时间戳用于端到端时延测量，实测小于 4 毫秒。

**机制上三板斧**（S9）：一是帧级超时丢弃——每帧独立 0.5 秒窗口，
未收齐整帧丢弃，丢包影响被锁定在单帧之内；二是增量节流上报——
变化超过阈值才真实发包，静默期兜底一次，带宽平稳不打尖峰；
三是端到端时延测量持续观测；四是 CRC 加长度一致性的双重校验，
连"重算 CRC 绕过"的对抗场景都能检出。

## 第四部分：实验验证（S10–S13，约 95 秒）

实验设计（S10）：对照实验，同帧率同编码，对 UDP 正常、UDP 丢包 50%、
TCP 三种方式测试，指标是帧率、端到端时延、丢包恢复与带宽占用。

数据（S11）：UDP 正常 19.64 帧、3.95 毫秒；丢包 50% 降到 9.56 帧，
时延几乎不变；TCP 19.51 帧且数据完整。

结果分析（S13）：关键在"为什么丢包一半、帧率就正好减半"——
丢包独立随机作用于每个数据报，完整帧比例的期望就是 (1-p)，
理论 10 帧与实测 9.56 高度吻合，说明模型可信。TCP 的问题在于队头阻塞：
重传期间后续字节无法交付，公网劣化时表现为卡顿。
而 UDP 丢一帧只黑一瞬，下一帧立即续播——
实时场景的"卡顿感"来自等待，而不是数据丢失本身。

## 第五部分：计网知识收获（S14–S17，约 45 秒）

这个项目把课本知识全部落了地：BLE 的 GAP 广播模型与 GATT 服务订阅；
UDP 分片重组——把传输层的职责在端侧实现；CRC 之外叠加长度一致性校验的
纵深防御；以及 NAT 中继穿透——两端主动注册、中继按房间转发，
无公网 IP 也能跨互联网互通。每一条都有实验或抓包证据支撑。

## 踩坑实录（S18–S19，约 30 秒）

三个真实的工程问题：headless 版 OpenCV 覆盖 GUI 版导致显示失败；
torch 与 numpy 2.x 断代导致检测静默返回空——这两个都是依赖断代与覆盖的坑；
摄像头设备独占导致图传中断——用"最新帧缓存"共享解决。
每个坑都定位到根因并写进了报告。

## 总结（S15–S16，约 40 秒）

回到科学问题：两个子问题都得到了机制级解决并经量化验证——
20 帧、小于 4 毫秒、50% 丢包不阻塞。我们也诚实标注边界：
心率数值来自模拟源，万级并发是设计目标尚未实测。

展望三条：GTP/2.0 引入 Mesh 与语音分片；LoRa 433 山区公里级信标；
语义通信——只传识别结果不传画面。

## 可能的提问与应答（新增 3 条）

**Q1：为什么 50% 丢包，帧率正好也掉一半左右？**
丢包独立随机作用于每个数据报，完整帧比例的期望是 (1-丢包率)，
理论 10 帧、实测 9.56 帧，与期望高度吻合。

**Q2：TCP 的粘包你们怎么处理的？**
TCP 是字节流没有消息边界。我们用 4 字节长度前缀定长分帧：
先读长度、再精确读满该长度的字节。

**Q3：RPA 是什么？为什么扫不到已绑定的手表？**
可解析随机地址——绑定设备为防追踪，地址每 15 分钟轮换、广播不含名称，
只有持有 IRK 密钥的已配对手机能解析。我们实测：两次独立 30 秒定向监听，
绑定的 GT5 零广播，对第三方设备完全隐身。

（原有 Q4 中继单点、Q5 对比 WebRTC 的应答保留不变）
