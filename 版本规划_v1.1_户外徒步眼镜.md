# 版本规划 v1.1 → v2.0：户外徒步 AI 眼镜

> 最终目标：无互联网的高山地带（秦岭等），多副眼镜之间**互联、语音通话、相互定位**；
> 无网时可与手机/手表互联；有网时接入 GLM 获取天气/地图并支持**离线地图**显示。
> 路线原则：每一步都先在"电脑 + 现成硬件"上仿真跑通，再向眼镜端迁移（沿用 v1.0 的成功路径）。

---

## 〇、一条主线

```
v1.0 已完成：局域网/公网图传 + GLM 视觉 + AR HUD（软件仿真）
    ↓
v1.1 手表互联：BLE 采集心率 → HUD 显示（GTP/1.0 遥测协议诞生）
    ↓
v1.2 眼镜互联：ESP32 协处理器 + ESP-NOW，眼镜间文本/坐标互通（GTP/2.0 Mesh）
    ↓
v1.3 语音与定位：Opus 语音对讲 + RSSI/GPS 相互定位
    ↓
v1.4 距离扩展：LoRa 433MHz 远距离文本/信标（公里级）
    ↓
v1.5 有网增强：GLM 天气/地图 API + 离线瓦片地图上屏
    ↓
v1.6 显示图层管理 + 语音控制：界面组件可独立开关，GLM 意图识别语音操控
    ↓
v2.0 硬件一体化：眼镜端从 PC 迁移到嵌入式主控
```

---

## 一、v1.1 手表互联（华为 GT5 心率上 HUD）

### 1.1 先搞清楚：GT5 能给我们什么数据

| 数据 | 通道 | 可行性 |
|---|---|---|
| **心率** | 标准 GATT 心率服务 `0x180D`（特征 `0x2A37` Notify） | ✅ 标准协议，大概率直接可读 |
| **电量** | 标准 GATT 电池服务 `0x180F`（`0x2A19`） | ✅ 直接可读 |
| 步数/血氧/睡眠 | 华为私有服务（需华为运动健康授权体系） | ⚠️ 无官方开放接口，**降级处理**：先扫描私有 UUID 尝试，拿不到就放弃，不要死磕 |
| 实时 GPS | 华为私有 | ⚠️ 同上；眼镜端自己配 GPS 模块才是正解（v1.3） |

**结论**：v1.1 只承诺**心率 + 电量**，这是标准蓝牙协议保证能拿到的；其余标注"探索项"。

### 1.2 技术方案

```
[华为 GT5]  ──BLE GATT──>  [眼镜端主机(PC/笔记本蓝牙)]  ──>  HUD 心率面板
   Central 角色: Python bleak 库
   订阅 0x2A37 Notify → 每 1s 收一次心率值
```

- 工具：`pip install bleak`（跨平台 BLE 库，Windows 原生支持）
- 注意：手表需与运动健康 App 断连或允许多连接时测试；蓝牙适配器需支持 BLE（近几年的笔记本都支持）

### 1.3 协议创新：GTP/1.0（Glasses Telemetry Protocol）

BLE 的 GATT 已经是标准协议，我们的"自研协议"定义在**应用数据格式层**——
定义"传感器遥测数据如何编码、压缩、校验、节流"，这是合理且有话可讲的创新点
（不必重造蓝牙底层，那是标准化组织的事）。

**GTP/1.0 帧格式（20 字节头 + 载荷，适配 BLE MTU 247）**：

```text
| ver 2b | type 6b | flags 4b | seq 4b | ts 4B | len 2B | payload 0~200B | crc16 2B |
```

| type | 含义 | payload 示例 |
|---|---|---|
| 0x01 | 心率 HR | 1 字节 bpm |
| 0x02 | 电量 BATT | 1 字节百分比 |
| 0x03 | 步数 STEPS | 4 字节 |
| 0x04 | 坐标 GPS | 纬度/经度/海拔/速度（v1.3） |
| 0x05 | 文本 TEXT | UTF-8（留言/信标） |
| 0x06 | 语音 VOICE | Opus 分片（v1.3） |

**三个创新设计点（答辩可讲）**：

1. **增量上报**：心率不变/变化 <2bpm 不发包，省电省带宽（手表侧遥测 80% 时间是冗余的）
2. **自适应节流**：静止 1 次/5s → 运动 1 次/1s，由 flags 位控制
3. **前向兼容 TLV**：未知 type 直接跳过不报错——未来加血氧/体温不用改协议版本

### 1.4 v1.1 验收标准

- [ ] 眼镜端 HUD 实时显示 GT5 心率（数字 + 心形跳动动效）
- [ ] 心率 > 150 或 < 50 触发红色告警闪烁
- [ ] 电量显示
- [ ] 断开手表 → 面板显示 "WATCH OFFLINE" 而非卡死
- [ ] GTP 帧 CRC 校验：人为改 1 位数据能被检出

---

## 二、v1.2 眼镜互联：无网状态下眼镜之间互通

### 2.1 关键选型（无互联网的设备间通信）

| 方案 | 距离 | 速率 | 功耗 | 成本 | 结论 |
|---|---|---|---|---|---|
| BLE 直连 | ~50m | ~700kbps | 极低 | 0（复用蓝牙） | 短距兜底 |
| **ESP-NOW**（乐鑫） | 视距 200~400m | ~500kbps | 低 | ESP32-C3 ≈15元 | **v1.2 主力** |
| WiFi 直连/热点 | ~100m | 高 | 高 | 0 | 图传可用 |
| LoRa 433MHz | **山区 5~15km** | ~5kbps | 极低 | SX1262 ≈25元 | **v1.4 远距文本** |

**决策**：加一块 **ESP32-C3 作为眼镜的"通信协处理器"**（挂在主控上，串口通信），
一石三鸟：ESP-NOW 眼镜间互联 + BLE 连手表/手机 + 未来挂 LoRa 模块。

### 2.2 GTP/2.0 Mesh：自研组网协议

- **组网方式**：简化按需路由——先全网洪泛（TTL=3），收到重复包丢弃；
  高负载时升级为"已知邻居表定向转发"
- **地址**：每副眼镜分配 2 字节短地址（如 0x0001），ESP32 MAC 自动映射
- **消息类型**：TEXT（留言）/ POS（坐标信标）/ PING/PONG（测距与在线表）/ VOICE（v1.3）
- **防风暴**：同包 5 秒内只转发一次（去重表）

### 2.3 验收标准

- [ ] 两副原型在无网环境互发文本，视距 100m 成功率 > 95%
- [ ] 在线表：能看到对方的短地址、最后坐标、心跳时间
- [ ] 断电重启后 10s 内自动重新入网

---

## 三、v1.3 语音对讲与相互定位

### 3.1 语音（技术要点）

```
PTT 按键 → 16kHz 采集 → Opus 编码(16~24kbps) → GTP/VOICE 分片(每片<200B)
→ ESP-NOW 发送 → 对端重组 → Opus 解码 → 扬声器
```

- 时延预算：编码 20ms + 传输 40ms + 抖动缓冲 60ms ≈ **端到端 < 200ms**（对讲机水平）
- Opus 用 ESP32 上移植版或主控软编码；帧长 40ms

### 3.2 相互定位（三层精度）

| 层 | 技术 | 精度 | 成本 |
|---|---|---|---|
| L1 存在 | 心跳在线表 | 知道"在网内" | 0 |
| L2 测距 | BLE/ESP-NOW RSSI 查表换算 | ±5~15m | 0 |
| L3 坐标 | GPS 模块（ATGM336H）+ Haversine 算距离 + 磁力计算方位 | ±3m / 方位±5° | ~15元 |

HUD 显示：`队友 0x0002 → 东南 120° 距离 380m`，这就是户外场景的核心价值。

---

## 四、v1.4 距离扩展：LoRa 远距离链路

- ESP32 + SX1262（433MHz，山区视距实测常见 5~15km）
- 只承载：GPS 信标（30s/次）+ 短文本（<100B）——带宽不够跑语音，语音仍走 ESP-NOW
- 意义：**翻过一道山梁也能知道队友位置**——这是 ESP-NOW 200m 做不到的

---

## 五、v1.5 有网增强：GLM + 天气 + 地图

- **天气**：和风天气/Open-Meteo API → GLM 生成一句自然语言播报（"3 小时后山顶有雨，建议下撤"）
- **地图**：
  - 有网：请求 OpenStreetMap 瓦片缓存到本地
  - 无网：预先下载区域瓦片；HUD 显示"航点 + 轨迹 + 队友位置"图层（比全地图渲染更现实）
- **GLM 融合**：把心率/海拔/天气/队友位置作为上下文喂给 GLM，输出**决策建议**（"你心率 165 且前方爬升 300m，建议休息"）——这是 AI 眼镜区别于普通对讲机的杀手级功能

---

## 六、v1.6 显示图层管理 + 语音控制（新增设想）

> 设想：显示端的所有界面组件（心率面板、告警横幅、AI 字幕、导航图层……）
> 都可以**独立开启/关闭**；后续接入语音，直接对 GLM 说"关掉心率面板"即可生效。

### 6.1 显示图层管理器（DisplayManager，纯软件，先做）

每个界面组件 = 一个可独立控制的**图层卡片**，统一注册到管理器：

| 图层 ID | 名称 | 默认 | 优先级 | 规则 |
|---|---|---|---|---|
| L1 | 告警横幅 | 开 | 最高 | **任何方式不可关闭**（安全兜底） |
| L2 | 心率面板 | 开 | 高 | 可语音/按键关闭 |
| L3 | AI 字幕 | 关 | 中 | 识别到语音时自动弹出 |
| L4 | 导航航点 | 关 | 中 | v1.5 地图数据接入后启用 |
| L5 | 系统状态栏 | 开 | 低 | 电量 < 15% 时自动隐藏其他图层省电 |

管理器接口：`register / show / hide / set_priority`——所有组件与主渲染循环解耦，
增删界面不改核心代码。

**计网知识（控制面/数据面分离的端侧版）**：图层管理器与渲染循环之间用本机
UDP/JSON **控制总线**通信——图层是"订阅者"，开关命令是"发布者消息"。
数据面（心率/视频流）与控制面（图层开关）分离，正是课本网络体系结构中
"控制面/数据面分离"思想在单机端侧的映射（SDN 的核心思想同源）。

### 6.2 语音接入（AI 意图识别）

```text
麦克风 → ASR 文字（讯飞/Whisper）→ GLM 意图识别
      → 结构化指令 {"action":"hide","target":"hr_panel"}
      → DisplayManager 执行 → TTS 确认："心率面板已关闭"
```

- 意图协议 GTC/1.0（Glasses Text Command）：与 GTP 同风格，`action + target` 二段式
- GLM 提示词约束**只输出 JSON**；解析失败即忽略（防幻觉误操作）
- 防误触：PTT 按键或唤醒词"小镜小镜"；连续两次确认高危操作

**计网知识：意图接口与权限设计（呼应 0908 实测）**
今日实测 Gadgetbridge 的意图接口用 signature 级权限保护，外部无法触发。
我们的语音控制运行在**本机回环**上（无需权限）；但未来若开放"手机远程控制
眼镜图层"，控制通道必须加 **Token 鉴权 + TLS**——否则任何人可关掉你的告警层。
今天踩的墙就是明天的设计依据。

### 6.3 验收标准

- [ ] 五个图层均可独立开关，告警层强制常显
- [ ] 语音"关闭心率面板"→ 面板消失 + TTS 确认，全程 < 3 秒
- [ ] 低电量自动进入省电图层方案
- [ ] 断网状态全功能可用（本地 ASR 或按键兜底）

---

## 七、v2.0 硬件一体化

- 主控：树莓派 Zero 2W / 安卓手机方案二选一（沿用 v1.0 结论）
- 通信板：ESP32-C3（BLE+ESP-NOW）+ SX1262（LoRa）二合一扩展板
- 显示：单目 OLED/棱镜模组
- 结构：3D 打印镜架 + 2000mAh 电池（目标续航 4h）

---

## 八、风险与降级预案

| 风险 | 概率 | 降级方案 |
|---|---|---|
| GT5 私有数据（步数/血氧）拿不到 | 高 | 已降级：只做心率+电量（标准协议保证） |
| ESP-NOW 两端必须都是乐鑫芯片 | 中 | 手机端临时用"串口透传 App"代替 |
| 语音时延/丢音 | 中 | 降级为"语音留言"（按一下发一段，非实时） |
| LoRa 模块调试难 | 中 | 先用两块开发板对发验证再上眼镜 |
| 秦岭实测成本高 | 低 | 先在市区高楼间测衰减，山区做最终验收 |

---

## 九、里程碑总表

| 版本 | 周期 | 一句话验收 |
|---|---|---|
| v1.0 ✅ | 已完成 | 局域网/公网图传 + GLM 识别 + AR HUD |
| v1.1 | 2~3 周 | GT5 心率实时上 HUD，GTP/1.0 定稿 |
| v1.2 | 3~4 周 | 无网两原型互通文本/坐标，GTP/2.0 Mesh |
| v1.3 | 3~4 周 | PTT 对讲 <200ms + 队友方位距离 |
| v1.4 | 2~3 周 | LoRa 5km+ 信标互通 |
| v1.5 | 2~3 周 | 天气播报 + 离线航点地图 |
| v1.6 | 2~3 周 | 五图层独立开关 + 语音控制 < 3 秒响应 |
| v2.0 | 持续 | 一体化真机 + 山区实地验收 |
