# 计算机网络知识点讲解 —— 结合 AI 眼镜项目

> 本文把项目里用到的所有计网知识整理成"课本概念 + 项目对应 + 实验数据 + 答辩话术"四段式，可以直接当复习提纲和答辩手册用。

---

## 〇、项目架构总览（先记住这张图）

```text
┌──────────────┐                          ┌──────────────┐
│   眼镜端      │ ①UDP :9600 图传+字幕      │  中继服务器    │
│ glasses_main │ ───────────────────────> │ relay_server │
│  (发送端)     │   REG/HBT 信令            │ (公网VPS)     │
└──────┬───────┘                          └──────┬───────┘
       │ ③HTTPS :443                             │ ②UDP :9600 转发
       ▼                                         ▼
┌──────────────┐                          ┌──────────────┐
│  GLM 云端     │                          │   显示端      │
│  视觉大模型    │                          │ (观看端,可N个) │
└──────────────┘                          └──────────────┘
```

- **数据面**：画面流走 UDP（眼镜端 → 中继 → 显示端）
- **控制面**：AI 问答走 HTTPS（眼镜端 → GLM 云端）
- 混合了 **C/S 模式**（调 API）与**转发分发**（中继）

---

## 一、应用层（第二章）

### 1.1 自定义应用层协议

**课本概念**：应用层协议定义报文格式、字段语义、交互时序（如 HTTP 的请求行/头部/正文）。

**项目对应**：`protocol.py` 的 20 字节帧头，就是一份微型应用层协议：

```text
| magic 2B | frame_id 4B | total 2B | seq 2B | payload_len 2B | ts_ms 8B |
   魔数        帧编号        总片数      序号      负载长度        发送时间戳
```

- **magic（魔数）**：校验报文合法性，防止无关流量破坏重组（类似 HTTP 响应开头的状态行）
- **frame_id / seq / total**：应用层的序号体系，让无结构的 UDP 字节流变成可重组的帧
- **ts_ms 时间戳**：测量端到端时延的依据

**答辩话术**：UDP 只负责把数据报扔出去，报文怎么切、怎么拼、怎么算时延，全是应用层协议的事——我自己设计并实现了一个。

### 1.2 TCP 粘包/拆包问题

**课本概念**：TCP 是字节流协议，没有消息边界；一次 send 可能被拆成多段到达，多次 send 也可能合并到达。

**项目对应**：TCP 模式下每帧前加 **4 字节长度前缀**（`struct.pack("!I", len)`），接收端先读 4 字节得长度，再循环 `recv` 直到读满——这是工程上标准的"**定长包头**"分帧法。

**答辩话术**：TCP 可靠但无边界，我用长度前缀自定义了消息边界；UDP 天然有边界（一个数据报一条消息），但有大小限制，所以我用分片协议弥补。

### 1.3 C/S 模式与 P2P 模式（第一章 概述）

| 模式 | 课本定义 | 项目体现 |
|---|---|---|
| C/S（客户/服务器） | 服务方公开地址被动等待，客户主动发起请求 | ① GLM API：眼镜端发 HTTPS 请求，云端应答；② TCP 模式：服务端 listen，客户端 connect |
| P2P | 双方对等，无固定服务器 | 直连模式下眼镜端与显示端直接互发 UDP |
| 混合架构 | 数据与控制分开走不同模型 | **本项目：图传走中继转发（数据面），AI 走云端 API（控制面）** |

### 1.4 应用层组通信（"房间"机制）

**课本概念**：一对多通信，网络层原生方案是 IP 组播（Class D 地址），但公网几乎不部署。

**项目对应**：`relay_server.py` 在**应用层**实现了组通信——服务器维护"房间表"，收到发送端数据后查表复制 N 份发给同房间所有观看端：

```python
rooms = {"demo": {"senders": [眼镜], "viewers": [显示端A, 显示端B, ...]}}
```

**答辩话术**：公网组播不可用，业界通行做法是把组播下沉到应用层实现；我的房间制转发就是最小可用原型，万级用户场景下演进为树形分发。

### 1.5 HTTP/HTTPS 调用云 API

**课本概念**：HTTP 基于 TCP、请求-响应模式、无状态。

**项目对应**：眼镜端调 `glm-4v-flash` 视觉大模型：把 JPEG 帧转 base64 放进 JSON 请求体 → POST 到智谱服务器 → 同一 TCP 连接上收到 JSON 回答。语音播报（TTS）在本地完成，不走网络——**有状态计算放云端、无状态输出放端侧**。

---

## 二、运输层（第五章）

### 2.1 UDP vs TCP（核心考点，有真实数据）

| 特性 | UDP | TCP |
|---|---|---|
| 连接 | 无连接 | 面向连接（三次握手） |
| 可靠性 | 尽最大努力交付，可能丢/乱序 | 可靠、有序、不丢不重 |
| 边界 | 面向报文，有边界 | 面向字节流，无边界（粘包） |
| 首部开销 | 8 字节 | 最少 20 字节 |
| 拥塞/流量控制 | 无 | 有，丢包时会**重传并降速** |
| 适场景 | 实时音视频、DNS | 文件传输、网页 |

**实验数据（本机实测）**：

| 实验 | 帧率 | 时延 | 现象 |
|---|---:|---:|---|
| UDP 正常 | 19.64 fps | 3.95 ms | 画面连续 |
| UDP 50% 丢包 | 9.56 fps | 4.02 ms | 跳帧但不阻塞 |
| TCP 对照 | 19.51 fps | — | 完整，但重传可能卡顿 |

**结论话术**：实时视频宁可丢旧帧也不能等重传——AR/VR 的旧画面到达时已失去价值，所以主协议选 UDP；TCP 作为可靠传输对照组。

### 2.2 UDP 之上的"应用层可靠性"（重点创新点）

**课本概念**：UDP 不保证到达、不保证顺序，可靠传输机制（确认、重传、排序、超时）在 TCP 内部实现。

**项目对应**：UDP 要传大帧（单帧 JPEG 可达 50KB+，超过单个数据报约 65507B 上限，且 IP 分片不可控），所以在应用层实现了"类 TCP 但只挑必要部分"的机制：

| TCP 内置机制 | 我的 UDP 应用层对应 | 设计取舍 |
|---|---|---|
| 序号 | frame_id + seq | 有 |
| 确认 ACK | **不做**（省反向带宽） | 实时视频容忍丢失，不值得确认 |
| 重传 | **不做**，改为**帧级超时丢弃**（0.5s 收不齐整帧就扔） | 旧帧不值得重传，实时性优先 |
| 排序 | 按 seq 拼接 | 有 |
| 流量控制 | 自适应码率（降 JPEG 质量/帧率） | 简化版 |

**答辩话术**：我没有照搬 TCP，而是按实时视频的需求**裁剪**出"有序 + 超时丢弃 + 无确认无重传"的轻量机制——这体现了运输层机制设计要服务应用需求的原则。

### 2.3 端口与套接字（Socket）

**课本概念**：IP 地址定位主机，**端口号定位主机上的进程**；Socket = (IP, Port) 通信端点。

**项目端口分配**：

| 端口 | 协议 | 用途 |
|---|---:|---|
| 9500 | UDP | 图传帧 |
| 9501 | UDP | AI 结果字幕 |
| 9600 | UDP | 中继服务器（信令+转发复用，靠前缀区分） |
| 443 | TCP | GLM API（HTTPS） |

**细节**：中继服务器一个端口同时承载信令和数据，用**报文前缀**（`REG|`/`HBT|`/其他）区分——这就是"**多路分用（demultiplexing）**"的应用层版本，课本里讲运输层靠端口号分用，应用层可以再自定义二级分用。

### 2.4 心跳与保活

**课本概念**：UDP 无连接，服务器不知道客户端是否还在线；TCP 也有 SO_KEEPALIVE 保活选项。

**项目对应**：客户端每 8 秒发 `HBT|`，服务器记录"最后活跃时间"，超过 30 秒未收到就删除该客户端——**超时判活**。这与 TCP 的Keepalive 探测、课本里"虚电路超时释放"是同一思想。

---

## 三、网络层（第四章）

### 3.1 IP 地址分类：私有地址与公网地址

**课本概念**：RFC1918 划定私有地址段（10/8、172.16/12、**192.168/16** 等），可重复使用、不在公网路由；公网地址全球唯一。

**项目对应**：`127.0.0.1` 是**回环地址**（loopback），数据不出网卡——所以直连模式只能本机通信。跨设备就要用局域网 IP（192.168.x.x），跨互联网就必须有公网 IP 或中继。

### 3.2 NAT（网络地址转换）——为什么要中继

**课本概念**：NAT 路由器把内网私有 IP:端口 映射成公网 IP:端口；**映射表由内网主机主动发起的出流量创建**，外部无法主动进入。

**项目对应（中继服务器存在的全部理由）**：

1. 眼镜端和显示端各自都在 NAT 后 → 谁也"找不到"谁
2. 两端都**主动**向公网中继发注册包 → 各自的 NAT 都打开了映射
3. 中继按注册信息把数据互相转发 → 通信建立

这就是标准的 "**NAT 穿透靠中继（relay）**" 方案，也是 STUN/TURN 体系里 TURN 的简化版。

### 3.3 路由与转发：数据面

**课本概念**：路由器查转发表，逐跳转发分组。

**项目对应**：中继服务器的"房间表"就是一张微型的**转发表**：

```
收到 来自(眼镜端, 房间demo) 的包 → 复制 → 发往 表中该房间所有观看端
```

万级用户时这张表会变成分布式的（Redis 房间路由 + 多中继节点），对应课本"路由协议让全网转发表保持一致"的规模化版本。

---

## 四、性能指标（第一章 1.3 节）

| 指标 | 定义 | 项目测法 | 实测值 |
|---|---|---|---|
| **时延** | 数据从源到目的的耗时 | 帧头时间戳 vs 接收时刻 | 直连约 2-4 ms；图传端到端 <200ms（局域网目标） |
| **吞吐量/带宽** | 单位时间传输的数据量 | 帧大小 × 帧率 | 50KB × 20fps ≈ **8 Mbps/路** |
| **丢包率** | 丢失分组占比 | 应用层模拟随机丢弃 | 50% 丢包 → 帧率 19.64→9.56 fps |
| **速率** | 比特流速度 | 发送帧率统计 | 20 fps |

**带宽账本（答辩杀手锏）**：单路 8Mbps，1000 个观看端 = 8Gbps 出口，单服务器物理不可能 → 必须房间化 + 树形分发（SFU）+ 边缘就近。这就是"为什么要分层演进出 v1→v3 架构"的定量依据。

**发送时延 vs 传播时延**：50KB 帧在 100Mbps 链路的发送时延 ≈ 4ms；本机传播时延近乎 0；公网传播时延 ≈ 距离/光速（跨国 ~150ms）。测量的 3.95ms 主要由发送时延+处理时延构成。

---

## 五、本项目知识点 × 课本章节速查表

| 课本章节 | 知识点 | 项目位置 |
|---|---|---|
| 第1章 概述 | C/S、P2P、性能指标、分组转发 | 架构总览、实验数据 |
| 第2章 应用层 | 自定义协议、HTTP(S)、DNS 不涉及、粘包分帧 | protocol.py、brain_api.py、TCP 模式 |
| 第3章 运输层 | UDP/TCP 对比、端口、Socket、可靠传输机制、保活 | protocol.py、capture 链路、HBT 心跳 |
| 第4章 网络层 | IP 分类、NAT、路由转发 | 中继方案、房间转发表 |
| 第5章 链路层 | 带宽/信道容量（背景） | 带宽账本、自适应码率动机 |

---

## 六、高频答辩问答（计网方向）

**Q1：为什么 UDP 丢包了你画面还能播？**
A：应用层做了帧级超时丢弃——一帧的分片超时未收齐直接扔掉，继续播后面的帧。丢的是"那一帧"，不是"整个流"。

**Q2：UDP 怎么保证帧完整？**
A：不保证，也**不应该**保证。我的协议只做"检错 + 排序 + 超时"，把完整性交给应用策略：实时场景里旧帧不值得等。

**Q3：TCP 也有序号确认，你的 frame_id/seq 和它什么关系？**
A：思想相同（序号+排序+超时），但我不做确认和重传。TCP 的可靠性是为"字节不丢"设计的，我的机制是为"帧要新"设计的——这是同一原理面向不同需求的裁剪。

**Q4：中继服务器会不会成为单点故障/瓶颈？**
A：会。单机中继是 v1 原型；规模化方案是多中继 + 房间调度（v2）+ 树形分发/SFU（v3），已在架构演进里规划，定量依据是 1000 观看端 = 8Gbps 出口的带宽账本。

**Q5：跨互联网时两边都在 NAT 后，数据怎么过来的？**
A：利用"NAT 映射由出流量创建"的特性：两端主动向公网中继注册，各自的 NAT 就打开了通道；中继按房间转发。这是 TURN 式中继方案的简化实现。

**Q6：AI 那部分走的什么协议？**
A：HTTPS（TCP 443），请求-响应模式，一问一答不保持长连接。属于 C/S 模式的典型应用。

**Q7：端到端时延怎么测的？**
A：发送端在协议头写入封装时刻（毫秒时间戳），接收端重组完成时用当前时间减去它。同一时钟域内测得 3.95ms；跨机需 NTP 同步。

---

## 七、一句话总结

> 这个项目用一条"实时视频流"串起了计网主干：**应用层自定义协议（分片/重组/时延测量）→ 运输层 UDP/TCP 机制取舍（序号、超时、丢弃、保活）→ 网络层 NAT 与中继转发（房间制组通信）→ 规模化架构演进（带宽定量分析驱动）**。每一层都不是背概念，而是为了解决眼前真实的问题被"逼"出来的。
